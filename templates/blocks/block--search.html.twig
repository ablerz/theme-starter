<aside{{ attributes.addClass('m-5', 'float-right') }}>
    {{ title_prefix }}
    {% if label %}
        <h2{{ title_attributes.addClass('text-light') }}>{{ label }}</h2>
    {% endif %}
    {{ title_suffix }}
    {% block content %}
        {{ content }}
    {% endblock %}
</aside>
